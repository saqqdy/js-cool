import { describe, expect, it } from 'vitest'

// safeParse and safeStringify now have their own test files:
// - test/safeParse.test.ts
// - test/safeStringify.test.ts

describe('safeJson placeholder', () => {
  it('is a placeholder - see safeParse.test.ts and safeStringify.test.ts', () => {
    expect(true).toBe(true)
  })
})
