/**
 * Sorter factory function
 *
 * @example
 * ```js
 * sorter('/name.png')
 * // { "suffix": "png", "type": "image" }
 *
 * sorter('/name.PDF')
 * // { "suffix": "pdf", "type": "pdf" }
 *
 * sorter('/name.xyz')
 * // { "suffix": "xyz", "type": "other" }
 * ```
 * @param url - file url
 * @returns result
 */
function sorter(url: string) {
	//
}

export default sorter
