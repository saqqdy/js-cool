/**
 * Sort Chinese by Chinese phonetic alphabet
 *
 * @since 5.14.0
 * @example
 * ```js
 * sortPinyin('/name.png')
 * // { "suffix": "png", "type": "image" }
 *
 * sortPinyin('/name.PDF')
 * // { "suffix": "pdf", "type": "pdf" }
 *
 * sortPinyin('/name.xyz')
 * // { "suffix": "xyz", "type": "other" }
 * ```
 * @param url - file url
 * @returns result
 */
function sortPinyin(url: string) {
	//
}

export default sortPinyin
